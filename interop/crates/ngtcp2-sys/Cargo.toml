[package]
name = "ngtcp2-sys"
version = "0.0.0"
edition = "2024"
rust-version = "1.96"
build = "build.rs"
links = "ngtcp2"
publish = false

[lib]
doctest = false

[dependencies]
aws-lc-sys = { version = "0.41", features = ["ssl"] }
libc = "0.2"

[build-dependencies]
bindgen = "0.72"
cmake = "0.1"
